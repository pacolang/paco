use stdlib::autodiff;
use vecmod;

#[differentiable]
fn loss(t: vecmod::T2<f32, 2>) -> f32 {
    let u = vecmod::T2<f32, 2>::zeros(2);
    let s = t.add(&u);
    s.sum()
}

fn main() {
    let t = vecmod::T2<f32, 2>::zeros(2);
    let (n, ga) = autodiff::grad(loss, t);
    print(n);
    print(ga.at(0));
    print(ga.at(1));
}
