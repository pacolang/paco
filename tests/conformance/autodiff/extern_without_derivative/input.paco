use stdlib::autodiff;

extern "C" {
    fn cbrt(x: float) -> float;
}

fn helper(x: float) -> float {
    unsafe { cbrt(x) }
}

#[differentiable]
fn f(x: float) -> float {
    helper(x) * 2.0
}

fn main() {
    let (v, d) = autodiff::grad(f, 8.0);
    print(d);
}
