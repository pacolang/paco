pub struct Linear {
    w: i64,
    pub b: i64,

    pub fn forward(&self) -> i64 {
        self.w + self.b
    }

    fn init_weights(&mut self) {
    }
}

fn main() {
    let layer = Linear { w: 2, b: 3 };
    print(layer.forward())
}
