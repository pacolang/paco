fn main() {
    let mut v: Vec<i64> = Vec::new();
    v.push(10);
    v.push(20);
    v.push(30);
    print(v.len());
    print(v[1]);
    match v.get(5) {
        Some(x) => print(x),
        None => print(0 - 1),
    }
}
