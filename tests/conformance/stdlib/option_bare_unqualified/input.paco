fn describe(x: Option<i64>) -> i64 {
    match x {
        Some(v) => v,
        None => 0 - 1,
    }
}

fn main() {
    let a: Option<i64> = Some(42);
    let b: Option<i64> = None;
    print(describe(a));
    print(describe(b))
}
