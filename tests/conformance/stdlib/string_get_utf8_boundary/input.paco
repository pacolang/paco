use stdlib::string;

fn main() {
    let s: string = "café";
    match s.get(0, 3) {
        Some(v) => print(v),
        None => print("none"),
    }
    match s.get(0, 4) {
        Some(v) => print(v),
        None => print("none"),
    }
}
