pub fn args() -> Vec<string> {
    let mut out: Vec<string> = Vec::new();
    let count = arg_count();
    let mut i = 0;
    while i < count {
        out.push(arg_at(i));
        i = i + 1;
    }
    out
}
